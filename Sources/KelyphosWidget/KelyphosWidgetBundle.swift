// KelyphosWidgetBundle.swift - Widget extension entry point
// Widget extension entry point — built via XcodeGen project, not SPM.

import SwiftUI
import WidgetKit

@main
struct KelyphosWidgetBundle: WidgetBundle {
    var body: some Widget {
        KelyphosWidget()
    }
}
